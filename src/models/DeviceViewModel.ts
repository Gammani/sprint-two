export type DeviceViewModel = {
    ip: string
    title: string
    lastActiveDate: string
    deviceId: string
}

export type SimpleDeviceViewModel = {
    deviceId: string
    iat: number
    exp: number
}